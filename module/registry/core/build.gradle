archivesBaseName = 'module.registry.core'

eclipse {
  project {
    name = 'module.registry.core'
  }
}

dependencies {
  compile project(':module/commons')
  compile project(':module/module')
  compile project(':module/zookeeper')
}
